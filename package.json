{
  "name": "namespace-monorepo",
  "private": true,
  "description": "Namespace ecosystem — modular kernel for structured apps",
  "scripts": {
    "test": "npm run test:namespace && npm run test:router && npm run test:plugin && npm run test:storage && npm run test:guards && npm run test:mvvm",
    "test:namespace": "cd namespace && npx vitest run",
    "test:router": "cd router && npx vitest run",
    "test:plugin": "cd plugin && npx vitest run",
    "test:storage": "cd storage && npx vitest run",
    "test:guards": "cd guards && npx vitest run",
    "test:mvvm": "cd mvvm && npx vitest run",
    "build": "npm run build:namespace && npm run build:router && npm run build:plugin && npm run build:storage && npm run build:guards && npm run build:mvvm && npm run build:docs",
    "build:namespace": "cd namespace && npx tsdown",
    "build:router": "cd router && npx tsdown",
    "build:plugin": "cd plugin && npx tsdown",
    "build:storage": "cd storage && npx tsdown",
    "build:guards": "cd guards && npx tsdown",
    "build:mvvm": "cd mvvm && npx tsdown",
    "build:docs": "cd docs && npx tsdown",
    "dev": "npm run build && express-reverse-proxy",
    "lint": "biome check .",
    "lint:fix": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.4",
    "@lopatnov/express-reverse-proxy": "^5.0.0"
  }
}
