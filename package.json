{
  "name": "namespace-monorepo",
  "private": true,
  "description": "Namespace ecosystem — modular kernel for structured apps",
  "scripts": {
    "test": "npm run test:namespace && npm run test:router && npm run test:plugin",
    "test:namespace": "cd namespace && npx vitest run",
    "test:router": "cd router && npx vitest run",
    "test:plugin": "cd plugin && npx vitest run",
    "build": "npm run build:namespace && npm run build:router && npm run build:plugin && npm run build:docs",
    "build:namespace": "cd namespace && npx tsdown",
    "build:router": "cd router && npx tsdown",
    "build:plugin": "cd plugin && npx tsdown",
    "build:docs": "cd docs && npx tsdown",
    "dev": "npm run build && express-reverse-proxy",
    "lint": "biome check .",
    "lint:fix": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.4",
    "@lopatnov/express-reverse-proxy": "^5.0.0"
  }
}
